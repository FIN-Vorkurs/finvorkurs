<% if user = current_user %>
  <%= render 'admin_bar' if user.role == User::ADMIN %>
  <%= link_to user.email, edit_user_path(user) %> |
   <%= link_to "Abmelden", logout_path %>
<% else %>
  <%= link_to "Anmelden", login_path %>
<% end %>
